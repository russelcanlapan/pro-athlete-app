specVersion: "v1"
serviceId: "proathlete"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "proathlete-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
